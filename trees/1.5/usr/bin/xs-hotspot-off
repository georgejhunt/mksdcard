#!/bin/bash 
# Turn off the hotspot capability of the xo1.5
if [ `id -u` -ne 0 ]; then
  echo "please become root to execute this command"
  exit 1
fi

rm -f /etc/xsce/access-point
source /usr/bin/xs-onboot

reboot
